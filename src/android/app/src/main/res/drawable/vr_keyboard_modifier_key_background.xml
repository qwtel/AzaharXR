<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/vr_loading_track"/> <!-- Pressed color -->
            <corners android:radius="4dp"/>
        </shape>
    </item>
    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/vr_azahar_center_dark"/> <!-- Default color -->
            <corners android:radius="4dp"/>
        </shape>
    </item>
</selector>
